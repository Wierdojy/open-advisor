const assetTypes = ['equity', 'etf', 'crypto', 'ipo_candidate'];
const themeStatuses = ['active', 'paused', 'archived'];
const eventTypes = ['earnings', 'filing', 'macro', 'listing', 'news', 'theme_update', 'custom'];
const reminderStates = ['open', 'done', 'snoozed'];
const sourceTiers = ['tier_1', 'tier_2', 'tier_3', 'tier_4'];

module.exports = {
  assetTypes,
  themeStatuses,
  eventTypes,
  reminderStates,
  sourceTiers
};
