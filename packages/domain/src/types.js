const assetTypes = ['equity', 'etf', 'crypto', 'ipo_candidate'];
const thesisStatuses = ['active', 'paused', 'archived'];
const catalystTypes = ['earnings', 'filing', 'etf_change', 'macro', 'listing', 'news', 'thesis_update'];

module.exports = {
  assetTypes,
  thesisStatuses,
  catalystTypes
};
